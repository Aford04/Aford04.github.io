// Stub: Fetch grade data (to be connected to PostgreSQL in future)
function fetchGradeData() {
    console.log("fetchGradeData() called");
}

// Stub: Populate the gradebook table
function populateGradebook() {
    console.log("populateGradebook() called");
}

// Trigger both stub functions on page load
fetchGradeData();
populateGradebook();
